# 部署指南

## 🚀 GitHub 上传步骤

### 前置准备

在上传到 GitHub 之前，请确保：

1. ✅ `.gitignore` 已正确配置（已排除敏感文件）
2. ✅ 数据库文件 `prisma/dev.db` 不会被上传
3. ✅ `.env.local` 环境变量文件不会被上传
4. ✅ `dist/` 构建产物目录不会被上传

### 上传命令

```bash
# 进入项目目录
cd app/AI1_Log

# 初始化 Git 仓库（如果尚未初始化）
git init

# 添加远程仓库
git remote add origin https://github.com/jaikensai888/Ai_weekly-report.git

# 添加所有文件到暂存区
git add .

# 提交代码
git commit -m "🎉 Initial commit: AI 智能周报助手"

# 推送到 GitHub
git push -u origin main
```

### 注意事项

#### 被排除的文件/目录

以下内容不会上传到 GitHub（由 `.gitignore` 控制）：

| 文件/目录 | 说明 |
|-----------|------|
| `node_modules/` | 依赖包目录 |
| `.next/` | Next.js 构建缓存 |
| `dist/` | Electron 打包输出 |
| `prisma/dev.db` | SQLite 数据库文件 |
| `.env*` | 环境变量文件 |

#### 敏感信息清单

| 类型 | 文件位置 | 处理方式 |
|------|----------|----------|
| DeepSeek API Key | `.env.local` | 已被 `.gitignore` 排除 |
| 用户数据 | `prisma/dev.db` | 已被 `.gitignore` 排除 |

## 📥 其他用户克隆后的设置

其他用户克隆项目后，需要执行以下步骤：

```bash
# 1. 安装依赖
npm install

# 2. 创建环境变量文件
cp config/env.example .env.local
# 编辑 .env.local，填入自己的 DeepSeek API Key

# 3. 初始化空数据库
npm run db:push

# 4. 启动项目
npm run dev
```

## 🔄 版本发布

### 创建新版本标签

```bash
# 创建标签
git tag -a v1.0.0 -m "Release v1.0.0"

# 推送标签
git push origin v1.0.0
```

### GitHub Release

1. 前往 GitHub 仓库页面
2. 点击 "Releases" -> "Create a new release"
3. 选择标签版本
4. 填写发布说明
5. 可上传 `dist/` 目录中的安装包作为 Release Assets

